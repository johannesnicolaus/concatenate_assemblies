{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/concatenate_assemblies/master/assets/schema_input.json",
    "title": "nf-core/concatenate_assemblies pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample_id": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample ID must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "fasta_file": {
                "type": "string",
                "pattern": "^\\S+\\.(fa|fasta|fas)(\\.gz)?$",
                "format": "file-path",
                "exists": true,
                "errorMessage": "FASTA file must be provided and have extension '.fa', '.fasta', '.fas' (optionally gzipped)"
            },
            "haplotype": {
                "type": "integer",
                "minimum": 1,
                "errorMessage": "Haplotype must be a positive integer"
            }
        },
        "required": ["sample_id", "fasta_file", "haplotype"]
    }
}